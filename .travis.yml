language: node_js

git:
    depth: 3

node_js:
    - "node"

install: yarn install

script:
    - yarn test
    - gulp

cache:
    yarn: true
    directories:
        - node_modules

notifications:
    email: false
